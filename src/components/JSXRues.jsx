const JSXRues = () => {
  return (
    <div>
      <h1 className="">JSX Rules</h1>
      <p>JSX must return a Single parent element</p>
      <p>JSX elements must be properly</p>
      <p>
        JSX attributes are written using CamelCase(e.g., className instead of
        class
      </p>
    </div>
  );
};
export default JSXRues;
